{
  "name": "library-frontend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "serve -s build -l 3000",
    "build": "node scripts/build.js"
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
